// habits.html has no other interactivity — this file exists solely to
// mark the roadmap's "good-habits" level complete on visit, matching the
// "completing a level means doing the exercise once" rule (here, "doing
// it" is reading the page).
(function () {
  if (typeof markRoadmapLevelComplete === "function") markRoadmapLevelComplete("good-habits");
})();
